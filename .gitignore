# OS noise
.DS_Store

# Local secrets — never commit (idea.md §12: local .env)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.hypothesis/

# codegraph's local code index — rebuilt per machine with `codegraph index`.
.codegraph/

# Local data stores and snapshots (idea.md §6/§7)
*.duckdb
*.duckdb.wal
